{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Quick Deploy",
            "type": "shell",
            "command": "powershell.exe",
            "args": ["-ExecutionPolicy", "Bypass", "-File", "deploy.ps1"],
            "group": "build"
        }
    ]
}
